#include <iostream>

int main() {
    int num;
    scanf("%d",&num);

    int array[num];
    for(int i=0;i<num;i++){
        if(i==0)
            continue;
            printf("%d",array[num-1]);

    return 0;
}

